# Drop brand assets here for pixel-perfect chrome.

## Needed from you

1. **aws-logo.svg** (or .png) — white AWS wordmark used in the dark top nav  
2. **aws-logo-orange.svg** — optional orange smile / full mark if separate  
3. Any favicon you want instead of the default

Place files as:

```
public/assets/aws-logo.svg
```

Until then the UI uses a built-in SVG approximation in `components/icons/AwsLogo.tsx`.
